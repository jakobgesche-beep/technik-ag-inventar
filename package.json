{
  "name": "technik-ag-inventar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute technik_ag_db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute technik_ag_db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
